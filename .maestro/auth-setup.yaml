# E2E Auth Setup Flow
# Injects a pre-authenticated session via deep link.
# Run e2e-setup.ts first to get the deep link URL, then set E2E_DEEP_LINK env var.
appId: com.irune.app
appTimeout: 15000
---
- launchApp:
    clearState: true
- openLink: ${E2E_DEEP_LINK}
- assertVisible: "ホーム|カレンダー"
